<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.cs307.evant.evant">

    <uses-sdk tools:overrideLibrary="com.firebase.ui.auth" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBy1Nv2rL8hSZnleE2IPl46sBvOzGED3GY" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NewEventActivity"
            android:label="@string/title_activity_new_event"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".loginPage"
            android:label="LoginPage" />
        <activity
            android:name=".DatabaseInterface"
            android:label="DatabaseInterface" />
        <activity
            android:name=".catList"
            android:label="catList" />
        <activity
            android:name=".eventList"
            android:label="eventList"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".signup"
            android:label="SignUpPage" />
        <activity
            android:name=".MapView"
            android:label="@string/title_activity_map_view"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ListView"
            android:label="@string/title_activity_list_view"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Profile"
            android:label="@string/title_activity_profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MapSelectView"
            android:label="@string/title_activity_map_select_view" />
        <activity
            android:name=".cat_lst"
            android:label="@string/title_activity_cat_lst" />
        <activity
            android:name=".eventPage"
            android:label="@string/title_activity_event_page"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>